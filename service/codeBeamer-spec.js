describe('codeBeamer', function () {

    beforeEach(module('ganttly'));

    it('should ...', inject(function ($codeBeamer) {

//        expect($codeBeamer.doSomething()).toEqual('something');

    }));

});
